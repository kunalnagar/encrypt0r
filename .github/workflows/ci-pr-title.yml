name: Continuous Integration (Lint PR Title)

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize

jobs:
  main:
    runs-on: ubuntu-18.04
    steps:
      - uses: kunalnagarco/action-semantic-pr@v1.4.7
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
